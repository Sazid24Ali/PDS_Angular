import { SocialProfilesEntity } from './social-profiles-entity';

describe('SocialProfilesEntity', () => {
  it('should create an instance', () => {
    expect(new SocialProfilesEntity()).toBeTruthy();
  });
});
